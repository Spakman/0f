<article id="editableArticle">
</article>

<nav>
  <ul>
    <% directories.each do |directory| %>
    <li><a href="<%= directory.uri_path %>"><%= directory.basename %></a></li>
    <% end %>
    <% pages.each do |page| %>
    <li><a href="<%= page.uri_path %>"><%= page.basename %></a></li>
    <% end %>
  </ul>
</nav>
